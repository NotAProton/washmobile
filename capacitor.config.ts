import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.washfront',
  appName: 'washfront',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
}

export default config
